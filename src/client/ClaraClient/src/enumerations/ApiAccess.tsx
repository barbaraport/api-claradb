export enum ApiAccess {
     host = "172.16.3.122",  // The IP Address of your server machine in your network, you can discover it with "ipconfig" in the command prompt
     port = "5000"  // The port number that the server is running, you can discover it when running the server
}